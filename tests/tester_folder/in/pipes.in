ls | rev
echo bonjour | wc
env | grep PATH

ls | < Makefile grep all
env | < Makefile grep all

ls > out | wc
cat out

< tests/var/var.c cat | < out rev
ls > out | < tests/var/var.c > out

ls | < Makefile cat > out | env | grep PATH | wc
cat out
rm out
